name: Legacy Academy PR 46 Runner Priority Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Reject legacy scheduling route
        run: |
          echo "This temporary runner-priority workflow is retired. Use .github/workflows/academy-zero-cost-sharded-completion.yml."
          exit 1
