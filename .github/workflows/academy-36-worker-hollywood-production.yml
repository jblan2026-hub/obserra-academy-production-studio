name: Legacy Academy 36 Worker Hollywood Production Disabled
run-name: legacy commercial Academy production disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    name: Confirm authoritative zero cost production path
    runs-on: ubuntu-24.04
    steps:
      - name: Explain safe production path
        shell: bash
        run: |
          set -euo pipefail
          echo "This legacy workflow is intentionally disabled."
          echo "It previously permitted paid model and commercial video provider execution."
          echo "The authoritative production path is .github/workflows/academy-zero-cost-sharded-completion.yml."
          echo "No model, media, database, deployment, or protected-content action was performed."
